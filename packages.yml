packages:
  # dbt-utils gives us cross-database date / type / surrogate-key macros.
  # See DISCIPLINES.md — these are the portable alternatives to Postgres-specific functions.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
